package main

func main() {
	// start rulescape server
	// nearby players request the rulescape
	// rulescape explains the protocol for trade requests
	// endpoint, transport protocol, encoding, data structure

	// start trade server
	// make have/want list to authorized identities
	// receive trade requests from nearby players
	// automatically reject requests from identity Anonymous
	// persist a record of the trade request (if rulescape permits logging)
	// notify me that a trade offer is available
	// I accept or reject
}
